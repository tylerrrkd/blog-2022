<div class="grid grid-flow-row grid-cols-3 gap-4 mb-2">
  {% for tag, _post in collections %}
    {% if tag !== "all" and tag !== "post" and tag !== "postsWithoutDrafts" %}
      <a class="tag mx-auto" href="{{ site.baseUrl }}tag/{{ tag }}">{{ tag }}</a>
    {% endif %}
  {% endfor %}
</div>
